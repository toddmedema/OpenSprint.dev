/**
 * @deprecated Tests moved to ExecutePhase.test.tsx — BuildPhase renamed to ExecutePhase per SPEED phase names.
 */
import { describe, it, expect } from "vitest";

describe("BuildPhase (deprecated)", () => {
  it("replaced by ExecutePhase.test.tsx", () => {
    expect(true).toBe(true);
  });
});
